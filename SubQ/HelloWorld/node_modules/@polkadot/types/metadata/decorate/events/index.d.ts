import type { MetadataLatest, PalletMetadataV14 } from '../../../interfaces';
import type { Registry } from '../../../types';
import type { Events } from '../types';
export declare function filterEventsSome({ events }: PalletMetadataV14): boolean;
/** @internal */
export declare function decorateEvents(registry: Registry, { lookup, pallets }: MetadataLatest, metaVersion: number): Events;
