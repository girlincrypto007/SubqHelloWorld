import type { MetadataLatest, PalletMetadataV14 } from '../../../interfaces';
import type { Registry } from '../../../types';
import type { Extrinsics } from '../types';
export declare function filterCallsSome({ calls }: PalletMetadataV14): boolean;
/** @internal */
export declare function decorateExtrinsics(registry: Registry, { lookup, pallets }: MetadataLatest, metaVersion: number): Extrinsics;
